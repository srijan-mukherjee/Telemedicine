fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
alembic==1.13.2
psycopg[binary]==3.2.11
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Phase 2 - authentication
bcrypt==4.2.0
pyjwt==2.9.0
python-multipart==0.0.9   # required by OAuth2PasswordRequestForm (login form parsing)
email-validator==2.2.0    # required by Pydantic EmailStr
reportlab==4.2.2            # phase 7

# Added in later phases (kept here so the file only needs light edits):
pgvector==0.3.4

# Phase 9 - AI symptom checker (RAG + Groq LLM)
groq==1.7.0
sentence-transformers==6.0.1
# NOTE: sentence-transformers needs torch. On machines without a GPU install
# the CPU-only wheel first to avoid the ~2GB CUDA bundle:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
